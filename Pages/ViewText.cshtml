@page
@model ViewTextModel
@{
    if(Model.ViewedFile != null)
    {
        ViewData["Title"] = "View Te Page";
    }
}

@if(Model.ViewedFile != null)
{
    // File View functionality still needs to be added
}